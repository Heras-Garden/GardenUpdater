# GardenUpdater Wiki

GardenUpdater keeps the Hera's Garden plugin family current from public GitHub Releases.

It checks release versions, validates release JARs, and stages newer builds in Paper's update folder. Paper applies the staged update during the next full server restart.

GardenUpdater never hot-reloads plugins and never restarts the server automatically.
